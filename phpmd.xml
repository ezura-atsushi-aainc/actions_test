<?xml version="1.0" encoding="UTF-8"?>
<ruleset name="Recommended PHPMD Ruleset">
    <description>
        PHPMD recommended ruleset for modern PHP projects.
    </description>

    <!-- 一般的に利用する全世界的によく使われる推奨ルールセット -->
    <rule ref="rulesets/cleancode.xml"/>
    <rule ref="rulesets/codesize.xml"/>
    <rule ref="rulesets/controversial.xml"/>
    <rule ref="rulesets/design.xml"/>
    <rule ref="rulesets/naming.xml"/>
    <rule ref="rulesets/unusedcode.xml"/>

    <!-- オプション: CyclomaticComplexityなど閾値を緩和/厳格設定したい場合 -->
    <rule ref="rulesets/codesize.xml/CyclomaticComplexity">
        <properties>
            <property name="reportLevel" value="10"/>
        </properties>
    </rule>

    <!-- オプション: LongMethodの警告閾値を変更したい場合 -->
    <rule ref="rulesets/codesize.xml/LongMethod">
        <properties>
            <property name="minimum" value="40"/>
        </properties>
    </rule>

    <!-- オプション: プロジェクトポリシーに合わせ除外ルールを記載可
    <exclude-pattern>tests/*</exclude-pattern>
    -->

</ruleset>
